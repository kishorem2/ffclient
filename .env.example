# Copy to .env and fill in. .env is gitignored — never commit it.
FANTASYPROS_API_KEY=your_key_here
FANTASYPROS_SEASON=2026
# Safety valve: script hard-stops at this many FP calls per day (limit is 50)
FP_DAILY_BUDGET=20
